<template>
    <!-- <div v-for="ship in ships" :key="`${ship.serie}-${ship.name}`">
        {{ ship.serie }} - {{ ship.name }}
    </div> -->
    <ShipDisplay/>
</template>

<script setup lang="ts">
</script>